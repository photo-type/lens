# Image Processing Service for PhotoType

## Requirements

You need the following installed:
- python@2.7
- opencv
- jupyter
- notebook
- numpy

The easiest way to install a separate environment is to use `virutalenv`